# Sort players by rank

The `utils` module provides a function which takes a list of players and returns a new list of the same players, but sorted

```py
-8<-- "examples/all_examples.py:sort-players-by-rank-demo"
```