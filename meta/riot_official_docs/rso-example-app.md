# RSO Example App (Node)

Source URL: https://static.developer.riotgames.com/docs/rso/rso-example-app.zip

The source is a zip archive. This local markdown file exists as a stable local link target from `lol.md`.

Expected contents in the upstream archive:

- Node.js sample server
- `/authorize` redirect example
- `/oauth2-callback` code exchange example
- token handling examples
